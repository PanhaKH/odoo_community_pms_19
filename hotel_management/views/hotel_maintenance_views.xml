<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_maintenance_request_form_inherit_hotel" model="ir.ui.view">
        <field name="name">maintenance.request.form.hotel.room</field>
        <field name="model">maintenance.request</field>
        <field name="inherit_id" ref="maintenance.hr_equipment_request_view_form"/>
        <field name="arch" type="xml">

            <!-- SAFE insertion inside first group -->
            <xpath expr="//sheet/group/group" position="inside">
                <field name="room_id"/>
                <field name="reservation_id" readonly="1"/>
            </xpath>

        </field>
    </record>

    <record id="action_hotel_maintenance_request" model="ir.actions.act_window">
        <field name="name">Maintenance Requests</field>
        <field name="res_model">maintenance.request</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- <menuitem id="menu_hotel_maintenance_requests" 
              name="Maintenance Requests" 
              parent="menu_hotel_root" 
              action="action_hotel_maintenance_request" 
              sequence="25"/> -->
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <record id="view_hotel_rate_plan_form" model="ir.ui.view">
        <field name="name">hotel.rate.plan.form</field>
        <field name="model">hotel.rate.plan</field>
        <field name="arch" type="xml">
            <form string="Rate Plan">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="bg-danger" invisible="active == True"/>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="e.g., Standard Rack Rate"/>
                        </h1>
                    </div>
                    <group>
                        <field name="active" invisible="1"/>
                    </group>
                    
                    <notebook>
                        <page string="Pricing Rules" name="pricing_rules">
                            <field name="line_ids">
                                <list editable="bottom">
                                    <field name="room_type_id"/>
                                    <field name="date_start" optional="show"/>
                                    <field name="date_end" optional="show"/>
                                    <field name="price" widget="monetary" optional="show"/>
                                    <field name="included_guests" optional="show"/>
                                    <field name="extra_person_fee" optional="show"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_hotel_rate_plan_tree" model="ir.ui.view">
        <field name="name">hotel.rate.plan.tree</field>
        <field name="model">hotel.rate.plan</field>
        <field name="arch" type="xml">
            <list string="Rate Plans">
                <field name="name"/>
                <field name="active" widget="boolean_toggle" optional="show"/>
            </list>
        </field>
    </record>

    <record id="action_hotel_rate_plan" model="ir.actions.act_window">
        <field name="name">Rate Plans</field>
        <field name="res_model">hotel.rate.plan</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first Master Rate Plan!
            </p>
        </field>
    </record>

    

</odoo>
